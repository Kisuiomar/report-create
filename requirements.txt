# Offline Dossier Pipeline Dependencies
# 100% Air-Gapped / Offline Capable

# Web API & CLI
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
typer[all]>=0.12.0
click>=8.1.7

# Data Validation & Serialization
pydantic>=2.7.0
pydantic-settings>=2.2.0

# HTTP Client for Local LLM (Ollama / vLLM)
httpx>=0.27.0
requests>=2.31.0

# Document Parsers
PyMuPDF>=1.24.0      # fitz - fast PDF parsing
pdfplumber>=0.11.0   # fallback PDF table extractor
pypdf>=4.2.0         # pure-python fallback PDF reader
python-docx>=1.1.0   # DOCX parser & exporter
openpyxl>=3.1.2      # XLSX parser & exporter
pandas>=2.2.0        # tabular data manipulation
beautifulsoup4>=4.12.0  # HTML sanitization & parsing
lxml>=5.2.0          # fast HTML/XML backend

# Install rapidocr-onnxruntime for pure onnx-based OCR without Tesseract
rapidocr-onnxruntime>=1.3.0; sys_platform != 'none' and python_version < '3.13'
pillow>=10.3.0

# Exporters
reportlab>=4.2.0     # PDF generation with custom styling
fpdf2>=2.7.8         # Lightweight PDF exporter fallback
# WeasyPrint>=61.0   # Optional HTML->PDF engine (requires GTK on Windows)

# Formatting & Logging
rich>=13.7.0
